# PRD: 하루 3문장 영어회화 대시보드

## 결론(요약)
매일 아침 출근길에 열어보는 개인용 대시보드로, **오늘의 영어 문장 3개 · 즐겨찾기(★) 모음 · 문장 출처 영상 링크**를 한 화면에서 보여주고, 연속 방문일수를 상단에 표시한다. 기존 "관심종목 대시보드"는 폐기하고, 동일한 기술 스택(순수 HTML/CSS/JS + `localStorage`, 파일 3개)으로 새로 만든다.

## 1. 목표
- 영어회화 공부 습관을 만들기 위해, 하루에 영어 문장 3개를 아침마다 노출한다.
- 매일 접속하도록 유도하기 위해 "연속 방문일수"를 시각적으로 보여준다.

## 2. 사용자
| 항목 | 내용 |
|---|---|
| 대상 | 나 (증권사 신입사원, 1인용) |
| 사용 시점 | 아침 출근길 (모바일/PC 브라우저) |
| 사용 빈도 | 매일 1회 이상 |

## 3. 핵심 기능
| # | 기능 | 설명 |
|---|---|---|
| 1 | 오늘의 영어 문장 3개 | 매일 날짜가 바뀌면 문장 풀(pool)에서 3개를 새로 뽑아 보여준다. 같은 날 재접속 시에는 동일한 3개가 유지된다. |
| 2 | 즐겨찾기(★) 모음 | 문장 카드에서 ★ 표시하면 "즐겨찾기 문장" 목록에 누적 저장되고, 언제든 다시 볼 수 있다. |
| 3 | 출처 영상/링크 | 각 문장에 출처(유튜브 링크 등)가 있으면 문장 카드에 링크로 표시해 클릭 시 새 탭에서 열린다. |

## 4. 화면 구성
| 영역 | 내용 |
|---|---|
| 상단 | "N일째 연속 방문 중" 표시 (오늘 처음 접속이면 카운트 증가, 하루 이상 건너뛰면 1로 초기화) |
| 좌측 패널 | 오늘의 영어 문장 3개 카드 (문장, 출처 링크, ★ 즐겨찾기 버튼) |
| 우측 패널 | 지금까지 ★ 표시한 문장 누적 목록 (문장, 출처 링크, ★ 해제 버튼) |

## 5. 데이터 모델 (localStorage)
| 키 | 값 형태 | 설명 |
|---|---|---|
| `ed.sentencePool` | `[{id, en, source: {label, url}}]` | 문장 원본 풀 (앱에 내장된 고정 데이터) |
| `ed.today.date` | `"YYYY-MM-DD"` | 마지막으로 문장을 뽑은 날짜 |
| `ed.today.sentenceIds` | `[id, id, id]` | 오늘 뽑힌 문장 3개의 id |
| `ed.favorites` | `[id, ...]` | 즐겨찾기한 문장 id 목록 |
| `ed.streak.count` | `number` | 연속 방문일수 |
| `ed.streak.lastVisit` | `"YYYY-MM-DD"` | 마지막 방문일 (스트릭 계산용) |

## 6. 기술 스택
- 프레임워크·빌드 도구 없이 순수 HTML/CSS/JS (`index.html`, `style.css`, `app.js` 3개 파일 유지)
- 데이터는 서버·백엔드 없이 브라우저 `localStorage`에 저장
- 문장 풀은 외부 API 연동 없이 `app.js` 내 고정 배열로 관리 (추후 필요 시 확장)

## 7. 검증 기준 (완료 정의)
브라우저에서 아래 3가지가 모두 동작하면 완성으로 간주한다.
1. 오늘의 영어 문장 3개가 화면에 표시된다.
2. ★ 표시한 문장이 즐겨찾기 목록에 누적되어 표시된다.
3. 문장 출처(영상/링크)를 클릭하면 해당 페이지로 이동한다.

## 8. 범위 제외 (Out of scope)
- 다중 사용자/로그인 기능
- 서버 기반 문장 추천 알고리즘, 외부 콘텐츠 API 연동
- 발음 평가, 음성 인식 등 학습 부가 기능
